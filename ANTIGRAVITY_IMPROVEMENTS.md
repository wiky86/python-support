# PyDataLab 개선 작업 지시서 (Antigravity용)

배포본(https://wiky86.github.io/python-support/) 점검 결과 발견된 버그와 개선 항목입니다.
아래 순서대로 진행해 주세요. 각 항목은 독립적이라 순서대로 하나씩 완료 후 다음으로 넘어가면 됩니다.

전제: 콘텐츠 JSON은 이미 확정본입니다. 콘텐츠 값을 바꾸지 말고, **표시(렌더링)·계산 로직**만 수정하세요. 단 1번(트랙1)은 교체용 파일이 별도 제공됩니다.

---

## 1. [콘텐츠 교체] 트랙1 해설·빈칸 표준화 — 제공 파일로 교체

**문제**
- 트랙1의 모든 빈칸(fillBlanks)·미션에 `explain`(해설) 필드가 없었음. 다른 트랙(2~7)은 전부 있음 → 트랙1에서 오답 시 학습자가 해설을 못 봄.
- 트랙1 첫 토픽(변수와 자료형)만 빈칸이 4개였음(다른 토픽 표준은 5개). 배포본에 "실습 4개"로 노출됨.

**작업**
- 별도 제공되는 `track1.zip`으로 기존 `content/tracks/track1/` 폴더를 **통째로 교체**하세요.
- 교체 후 트랙1 전 토픽이 "빈칸 5개 + 각 빈칸 explain 포함", 프로젝트 7개 미션도 explain 포함 상태가 됩니다.
- 변수와 자료형 토픽에 5번째 빈칸(int 자료형 확인, `type(count)`)이 추가되었으니, "실습 4개" 표기가 자동으로 "실습 5개"로 바뀌는지 확인하세요(개수를 하드코딩하지 말고 `fillBlanks.length`로 렌더링).

**완료 기준**
- 트랙1 variables 토픽 상세에서 실습이 5개로 표시됨.
- 트랙1 어느 토픽이든 빈칸 오답 시 해설이 노출됨(트랙2와 동일 동작).

---

## 2. [버그] 헤더 레벨/XP 표시 오류 — "Lv.10 XP"

**문제**
모든 페이지 헤더에 `Lv.10 XP`로 고정 표시됨. 진도 0인 게스트는 Lv.1이어야 하고, XP 숫자(0)도 누락됨. (본문 카드에는 "Lv.1 / 0 XP"로 올바르게 나오므로, 헤더 컴포넌트만 문제.)

**원인 추정**
헤더가 `getLevel(xp)`를 호출하지 않고 상수(10)를 쓰거나, 표시 문자열 조립에서 값이 빠짐.

**작업**
- 헤더의 레벨·XP 표시를 본문 카드와 동일한 `getLevel(xp)` / 실제 xp 값에 연결하세요.
- 레벨 공식은 `content/config/xp-rules.json`의 확정값을 사용합니다.
  - Lv(n)→Lv(n+1) 필요 XP = `200 + 50n`
  - `getLevel(xp)`: `level = Math.max(1, Math.floor((-175 + Math.sqrt(175*175 + 100*(xp+200))) / 50) + 1)`
- 표시 포맷 예: `Lv.{level} · {xp.toLocaleString()} XP` (현재는 "Lv.10 XP"로 XP 숫자 자체가 빠져 있으니 숫자도 반드시 포함).

**완료 기준**
- 게스트(xp=0) 상태에서 헤더가 `Lv.1 · 0 XP`로 표시됨.
- XP를 쌓으면 헤더 레벨이 본문 카드 레벨과 항상 일치함.

---

## 3. [버그] 배지 조건 문구에 원시 데이터 노출

**문제**
배지 도감(/badges)의 조건 설명이 사람이 읽을 문장이 아니라 내부 필드명·연산자를 그대로 출력함.
- "조건: 완료한 토픽 수가 **gte** 이상" ← 연산자 gte 노출
- "조건: **trackId** 트랙의 모든 토픽 완료" ← 파라미터명 노출
- 임계값(3, 7, 50 등)이 조건 문구에 안 들어감

**작업**
`badges.json`의 `condition`(type + 파라미터)을 사람이 읽을 한국어로 변환하는 함수 `describeBadgeCondition(condition)`을 만들어 조건 표시에 사용하세요.
조건 타입은 아래 9종이 전부입니다(현재 24개 배지 모두 이 안에 들어감).

| type | 파라미터 | 출력 문구 예시 |
|---|---|---|
| `topic_count` | `gte` | "토픽 {gte}개를 완료하면 획득" |
| `quiz_pass_count` | `gte` | "퀴즈 {gte}개를 통과하면 획득" |
| `project_count` | `gte` | "미니 프로젝트 {gte}개를 완료하면 획득" |
| `perfect_quiz_count` | `gte` | "퀴즈를 {gte}번 만점 통과하면 획득" |
| `topic_percent` | `gte` | "전체 토픽의 {gte}%를 완료하면 획득" |
| `streak` | `gte` | "{gte}일 연속 접속하면 획득" |
| `level` | `gte` | "레벨 {gte}에 도달하면 획득" |
| `track_complete` | `trackId` | "{트랙 한글명}의 모든 토픽을 완료하면 획득" |
| `all_tracks_complete` | (없음) | "모든 트랙을 완주하면 획득" |
| `flawless_track` | (없음) | "한 트랙의 모든 퀴즈를 만점 통과하면 획득" |

`track_complete`의 `{트랙 한글명}`은 trackId로 `track.json`의 title을 조회해 넣으세요(하드코딩 금지). 매핑: track1=파이썬 기초 다지기, track2=NumPy로 수치 다루기, track3=pandas 핵심 - 데이터 구조, track4=pandas 실전 - 데이터 가공, track5=그룹화와 집계, track6=데이터 시각화, track7=종합 분석 프로젝트.

**참고: 현재 badges.json condition 전수(24개)**
```
first_topic      topic_count gte=1
first_quiz       quiz_pass_count gte=1
first_project    project_count gte=1
track1_done~track7_done   track_complete trackId=track1..track7
halfway          topic_percent gte=50
all_tracks       all_tracks_complete
all_projects     project_count gte=7
perfect_1        perfect_quiz_count gte=1
perfect_10       perfect_quiz_count gte=10
flawless_track   flawless_track
streak_3/7/30/90/180   streak gte=3/7/30/90/180
level_5/10/12    level gte=5/10/12
```

**완료 기준**
- /badges의 모든 배지 조건이 자연스러운 한국어 문장으로 표시됨(gte, trackId 같은 원시 문자열이 화면에 없음).
- 임계값이 문구에 포함됨(예: "3일 연속 접속하면 획득").

---

## 4. [버그] 미니 프로젝트 제목이 전부 "실전 데이터 분석 프로젝트"로 동일

**문제**
학습 지도(홈)의 각 트랙 카드에서 미니 프로젝트가 모두 "실전 데이터 분석 프로젝트"라는 하드코딩된 문구로 표시됨. 실제 `project.json`에는 트랙별 고유 제목이 있음.

**작업**
학습 지도의 프로젝트 카드가 하드코딩 문구 대신 각 트랙 `project.json`의 `title`을 읽어 표시하도록 수정하세요.

**참고: 트랙별 실제 프로젝트 제목**
- track1: 학생 점수 분석기
- track2: 기온 데이터 통계 요약
- track3: 학생 성적표 분석
- track4: 지저분한 회원 데이터 정제하기
- track5: 매출 데이터 지점별·월별 요약
- track6: 매출 데이터를 그래프 3종으로 표현
- track7: 카페 데이터 종합 분석

**완료 기준**
- 홈 화면 7개 트랙의 미니 프로젝트 카드가 각기 다른 고유 제목으로 표시됨.
- (선택) 잠금 상태 안내 문구 "모든 토픽 완료 시 잠금 해제"는 유지.

---

## 5. [UX] 로그인 페이지의 개발용 안내 노출

**문제**
/login 상단에 "현재 `.env.local`에 실제 Supabase 키가 설정되지 않아 게스트 모드로 동작합니다. Supabase 대시보드의 URL과 anon key를..." 같은 **개발자용 기술 안내**가 최종 사용자에게 그대로 보임.

**작업**
- Supabase 키/`.env.local` 관련 기술 문구는 화면에서 제거하고, 필요 시 `console.info`로만 남기세요.
- 사용자에게는 친화적 문구로 대체: 예) "로그인 없이도 학습 진도가 이 브라우저에 자동 저장됩니다. 로그인하면 여러 기기에서 이어서 학습할 수 있어요."
- 게스트 모드 동작 자체는 그대로 유지(로컬 스토리지 저장).

**완료 기준**
- /login에 `.env.local`, `anon key`, `Supabase 대시보드` 같은 기술 용어가 노출되지 않음.
- 게스트 모드 안내가 사용자 언어로 표시됨.

---

## 6. [UX] 헤더 스트릭 "0일" 레이블 모호

**문제**
헤더에 맥락 없이 "0일"만 표시되어 무엇을 뜻하는지 불명확.

**작업**
- 불꽃(🔥) 아이콘을 앞에 붙이거나, hover 툴팁 "연속 학습 일수"를 추가하세요.
- 표기 예: `🔥 0일` + 툴팁 "연속 학습 {n}일".

**완료 기준**
- 헤더의 일수 표시가 "연속 학습"을 뜻함을 사용자가 알 수 있음.

---

## 7. [SEO/UX] 페이지별 `<title>` 동일

**문제**
모든 페이지의 브라우저 탭 제목이 "PyDataLab — 파이썬 데이터 분석 학습 공간"으로 동일.

**작업**
Next.js 메타데이터로 페이지별 타이틀을 설정하세요.
- 토픽 상세: `{토픽명} · PyDataLab` (예: "변수와 자료형 · PyDataLab")
- 배지 도감: `배지 도감 · PyDataLab`
- 로그인: `로그인 · PyDataLab`
- 홈: 현재 문구 유지

**완료 기준**
- 각 페이지 브라우저 탭·북마크 제목이 구분됨.

---

## 작업 우선순위 요약
1. 트랙1 파일 교체 (제공 zip)  ← 콘텐츠
2. 헤더 Lv.10 버그  ← 최우선 버그
3. 배지 조건 문구 변환  ← 최우선 버그
4. 프로젝트 제목 표시
5. 로그인 개발 안내 정리
6. 스트릭 레이블
7. 페이지 타이틀

2·3번이 사용자에게 가장 눈에 띄는 버그이니 파일 교체(1) 후 먼저 처리하세요.
